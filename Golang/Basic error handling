package main

import "fmt"

func divide(a, b float64) (float64, error) {
	if b == 0 {
		return 0, fmt.Errorf("Denominator shouldnot be 0")
	}
	return a / b, nil
}

func main() {
	div, err := divide(10, 0)
	if err != nil {
		fmt.Println("Error")
	}
	fmt.Println("The ans is:", div)
}
